- job:
    name: test-lwhsu-build-in-jail
    defaults: global
    scm:
      - FreeBSD-doc-head
    builders:
      - checkout-scripts
      - setup-jail
      - execute-in-jail
    publishers:
      - clean-jail
    wrappers:
      - timeout:
          timeout: 1
          timeout-var: 'BUILD_TIMEOUT'
          fail: true
          type: absolute
