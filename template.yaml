- job-template:
    defaults: global
    name: 'FreeBSD-{branch}-{arch}-build'
    node: jailer_fast
    scm:
      - 'FreeBSD-src-{branch}'
#    triggers:
#      - pollscm: H H/2 * * *
    properties:
      - inject:
          properties-content: |
            FBSD_BRANCH={branch}
            MOUNT_REPO=src
    builders:
      - checkout-scripts
      - setup-jail
      - execute-in-jail
    publishers:
      - scan-clang-warnings
      - ftp:
          site: 'artifact.ci.freebsd.org'
          target: 'snapshot'
          source: 'src/release/artifact/**'
          remove-prefix: 'src/release/artifact'
      - clean-jail
#      - mail-notify
      - trigger-parameterized-builds:
        - project:
            - 'FreeBSD-{branch}-{arch}-images'
            - 'FreeBSD-{branch}-{arch}-test_image'
            - 'FreeBSD-{branch}-{arch}-LINT'
          condition: SUCCESS
          svn-revision: true
          property-file: trigger.property
    wrappers:
      - timestamps
      - timeout:
          timeout: 60
          timeout-var: 'BUILD_TIMEOUT'
          fail: true
          type: absolute

- job-template:
    defaults: global
    name: 'FreeBSD-{branch}-{arch}-LINT'
    node: jailer_fast
    scm:
      - 'FreeBSD-src-{branch}'
#    triggers:
#      - pollscm: H H/2 * * *
    properties:
      - inject:
          properties-content: |
            MOUNT_REPO=src
    builders:
      - checkout-scripts
      - setup-jail
      - execute-in-jail
    publishers:
      - scan-clang-warnings
      - clean-jail
#      - mail-notify
    wrappers:
      - timestamps
      - timeout:
          timeout: 60
          timeout-var: 'BUILD_TIMEOUT'
          fail: true
          type: absolute

- job-template:
    defaults: global
    name: 'FreeBSD-{branch}-{arch}-images'
    node: jailer
    parameters:
      - string:
          name: SVN_REVISION
          description: "Subversion revision"
    builders:
      - checkout-scripts
      - setup-jail
      - execute-in-jail
    publishers:
      - ftp:
          site: 'artifact.ci.freebsd.org'
          target: 'snapshot'
          source: 'artifact/**'
          remove-prefix: 'artifact'
      - clean-jail
    wrappers:
      - timestamps
      - timeout:
          timeout: 30
          timeout-var: 'BUILD_TIMEOUT'
          fail: true
          type: absolute

- job-template:
    defaults: global
    name: 'FreeBSD-{branch}-{arch}-test_image'
    node: jailer
    parameters:
      - string:
          name: SVN_REVISION
          description: "Subversion revision"
    builders:
      - checkout-scripts
      - setup-jail
      - execute-in-jail
    publishers:
      - ftp:
          site: 'artifact.ci.freebsd.org'
          target: 'snapshot'
          source: 'artifact/**'
          remove-prefix: 'artifact'
      - clean-jail
      - trigger-parameterized-builds:
        - project:
            - 'FreeBSD-{branch}-{arch}-test'
          condition: SUCCESS
          svn-revision: true
          property-file: trigger.property
    wrappers:
      - timestamps
      - timeout:
          timeout: 30
          timeout-var: 'BUILD_TIMEOUT'
          fail: true
          type: absolute

- job-template:
    defaults: global
    name: 'FreeBSD-{branch}-{arch}-test'
    builders:
      #- cleanup-workspace
      - checkout-scripts
      - execute-job-build-script
    publishers:
      - publish-junit-results
#      - mail-notify

- job-group:
    name: 'FreeBSD-{branch}-{arch}-ci'
    jobs:
       - 'FreeBSD-{branch}-{arch}-build'
       - 'FreeBSD-{branch}-{arch}-images'
       - 'FreeBSD-{branch}-{arch}-LINT'
       - 'FreeBSD-{branch}-{arch}-test_image'
       - 'FreeBSD-{branch}-{arch}-test'
