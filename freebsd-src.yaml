- project:
    name: FreeBSD-amd64
    arch:
      - amd64
    branch:
      - head
      - stable-11
      - stable-10
      - stable-9
    jobs:
      - 'FreeBSD-{branch}-{arch}-ci'

- project:
    name: FreeBSD-i386
    arch:
      - i386
    branch:
      - head
      - stable-11
      - stable-10
      - stable-9
    jobs:
      - 'FreeBSD-{branch}-{arch}-ci'

- project:
    name: FreeBSD-arm64
    arch:
      - arm64
    branch:
      - head
      - stable-11
    jobs:
      - 'FreeBSD-{branch}-{arch}-ci'

- project:
    name: FreeBSD-riscv64
    arch:
      - riscv64
    branch:
      - head
      - stable-11
    jobs:
      - 'FreeBSD-{branch}-{arch}-ci'

- job:
    name: FreeBSD-head-scan_build
    defaults: global
    node: jailer_slow
    scm:
      - FreeBSD-src-head
    builders:
      - checkout-scripts
      - setup-jail
      - execute-in-jail
    publishers:
      - clean-jail
      - publish-clang-scan-build-results
    wrappers:
      - timestamps
